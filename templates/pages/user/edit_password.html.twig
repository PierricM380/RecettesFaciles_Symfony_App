{% extends 'base.html.twig' %}

{% block title %}Mofication mot de passe -
{{ parent() }}
{% endblock %}

{% block body %}
<!-- HEADER -->
<header class="row shadow" data-aos="fade-down">
	<section class="col-12">
		<h1><strong>Modifier mon mot de passe</strong></h1>
	</section>
</header>
<!-- MAIN -->
<section class="informations row">
	<div class="col-11 col-lg-5">
		{% for message in app.flashes('warning') %}
		<div class="alert alert-warning mt-4">
			{{ message }}
		</div>
		{% endfor %}

		<div class="card shadow">
			<div class="card-body">
				{{ form_start(form) }}
				<div class="form-group">
					{{ form_widget(form.plainPassword) }}
					<div class="form-error">
						{{ form_errors(form.plainPassword) }}
					</div>
				</div>

				<div class="form-group">
					{{ form_label(form.newPassword) }}
					{{ form_widget(form.newPassword) }}
					<div class="form-error">
						{{ form_errors(form.newPassword) }}
					</div>
				</div>

				<div class="form-group pt-4">
					<button type="submit" class='validate shadow me-4'>Valider</button>
					<a href="{{ path('home.index') }}" class="button delete shadow">
						Annuler
					</a>
				</div>
				{{ form_end(form) }}
			</div>
		</div>
	</div>
</section>
{% endblock %}